export const metadata = {
  title: "Our Team",
  description: "Meet the designers, engineers, strategists, and creators shaping the future at Studio Dezu.",
};

export default function TeamLayout({ children }) {
  return children;
}
